% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{tidypredict_save}
\alias{tidypredict_save}
\alias{tidypredict_load}
\title{Save and re-load a parsed model}
\usage{
tidypredict_save(x, file)

tidypredict_load(file)
}
\arguments{
\item{x}{A fitted model, or a parsed model from \code{\link[=parse_model]{parse_model()}}. Fitted
models are parsed before being saved.}

\item{file}{Path to write the YAML file to, or read it from.}
}
\value{
\code{tidypredict_save()} returns \code{x}, invisibly, so it can be used in a pipe.
\code{tidypredict_load()} returns a parsed model object.
}
\description{
\code{tidypredict_save()} writes a parsed model to a YAML file, and
\code{tidypredict_load()} reads one back. Together they persist a model's
prediction formula without needing the original model object, or the package
that fitted it, to be available later.

Use these rather than calling \code{\link[yaml:write_yaml]{yaml::write_yaml()}} directly. \code{yaml} defaults
to 7 significant digits, which is not enough to represent a split threshold
exactly: a re-loaded tree model can then send rows down a different branch
than the model it was saved from.
}
\examples{
model <- lm(mpg ~ wt + cyl, data = mtcars)

path <- tempfile(fileext = ".yml")
tidypredict_save(model, path)

loaded <- tidypredict_load(path)
tidypredict_fit(loaded)

}
