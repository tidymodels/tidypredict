# non-Gaussian families error with clear message

    Code
      tidypredict_fit(model)
    Condition
      Error in `mboost_check_regression()`:
      ! Only the `Gaussian()` family is supported for blackboost.
      i Family "Negative Binomial Likelihood (logit link)" applies a non-linear response function that cannot be expressed as a single formula.

---

    Code
      parse_model(model)
    Condition
      Error in `mboost_check_regression()`:
      ! Only the `Gaussian()` family is supported for blackboost.
      i Family "Negative Binomial Likelihood (logit link)" applies a non-linear response function that cannot be expressed as a single formula.

