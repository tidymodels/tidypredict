# Extract class probability trees for bagger models

Returns one list of per-tree expressions for each outcome level. For use
in orbital package.

## Usage

``` r
.extract_bagger_classprob(model)
```

## Arguments

- model:

  A bagger model object (classification)
