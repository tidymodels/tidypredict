# Extract multiclass linear predictors for earth models

For use in orbital package.

## Usage

``` r
.extract_earth_multiclass(model)
```

## Arguments

- model:

  An earth model object with multiple classes (glm.list with \>1
  elements)
